{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/homepage.css') }}">
{% endblock %}
{% block title %}YoYo{% endblock %}
{% block body %}
    <div class="container">

        <!-- Jumbotron Header -->
        <header class="jumbotron my-4 imgbackground" style="background-image: url('{{ asset('images/nonlavietnam.jpg') }}');">

            <h1 class="display-3">A Warm Welcome!</h1>
            <p class="lead">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Ipsa, ipsam, eligendi, in quo sunt possimus non incidunt odit vero aliquid similique quaerat nam nobis illo aspernatur vitae fugiat numquam repellat.</p>
            <a href="{{ path('app_post_new') }}" class="btn btn-primary btn-lg">Create a new post !</a>

        </header>

        <!-- Page Features -->
        <div class="row text-center">
            {% for post in post %}
            <div class="col-lg-3 col-md-6 mb-4">
                <div class="card h-100">

                    {#if no image#}
                    {% if post.imagePath == 'uploads/post/image/' %}

                        <div>
                            <img class="card-img-top" src="{{ asset('uploads/post/default_img.jpeg') | imagine_filter('card_thumbnail_homepage')}}" alt="">

                            {% set finish = post.finishAt %}
                            {% set now = "now"|date("m/d/Y") %}
                            {% set diff = date(finish).diff(date(now)) %}
                            {% set leftDays = diff.days %}
                            {% if leftDays >10 %}
                                <div class="dayCountdownWhite rounded">J-{{ leftDays }}</div>
                            {% else %}
                                <div class="dayCountdownRed rounded">J-{{ leftDays }}</div>
                            {% endif %}

                        </div>

                    {% else %}
                            <img class="card-img-top" src="{{ asset(post.imagePath) | imagine_filter('card_thumbnail_homepage')}}" alt="">

                        {% set finish = post.finishAt %}
                        {% set now = "now"|date("m/d/Y") %}
                        {% set diff = date(finish).diff(date(now)) %}
                        {% set leftDays = diff.days %}
                        {% if leftDays >10 %}
                            <div class="dayCountdownWhite rounded">J-{{ leftDays }}</div>
                        {% else %}
                            <div class="dayCountdownRed">J-{{ leftDays }}</div>
                        {% endif %}
                        {% endif %}
                    <div class="card-body">
                        <h5 class="card-title text-left">{{ post.title }}</h5>
                        <p class="card-text text-left">{{ post.content|escape('html') }}</p>
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('show_post', {uniquekey: post.uniquekey}) }}" class="btn btn-primary">Xem chi tiết</a>
                    </div>
                </div>
            </div>
            {% endfor %}

        </div>
        <!-- /.row -->

    </div>
{% endblock %}